spring:
  flyway:
    enabled: false
  datasource:
    url: "jdbc:postgresql://localhost:5432/flyway"
    username: "flyway"
    password: "flyway"
  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect: "org.hibernate.dialect.PostgreSQLDialect"
    hibernate:
      ddl-auto: validate